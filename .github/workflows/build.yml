name: Find Android Project
on: [push]
jobs:
  find:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Locate Android Build File
        # Szukamy pliku, który deklaruje plugin androida
        run: grep -r "com.android.application" .
